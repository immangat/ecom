/**
 * Actions to tell the reducer to either the change the open state, or
 * to change the items in the cart.
 * @type {{SET_CART_ITEMS: string, SET_OPEN_STATE: string}}
 */
export const CART_ACTIONS_TYPES = {
    SET_OPEN_STATE: "SET_OPEN_STATE",
    SET_CART_ITEMS: "SET_CART_ITEMS"
}